[project]
name = "agent-loop-demo"
version = "0.1.0"
description = "Deterministic restaurant recommendation agent runtime demo"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

