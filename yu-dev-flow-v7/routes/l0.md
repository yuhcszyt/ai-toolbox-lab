# L0 — Direct

Use L0 when the goal, constraints, existing pattern, and scope are clear and no decision blocks implementation.

Handoff:

1. Invoke `/implement` with the goal, scope, constraints, existing pattern, and minimum-implementation rules from the parent skill.

Do not invoke `/setup-matt-pocock-skills`, `/grill-with-docs`, `/to-spec`, `/to-tickets`, or `/wayfinder` for L0. A trivial change does not need extra setup or tests; non-trivial new logic still needs one minimal runnable check.
